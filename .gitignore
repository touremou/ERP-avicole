*.log
.DS_Store
.env
.env.backup
.env.production
.phpactor.json
.phpunit.result.cache
/.fleet
/.idea
/.nova
/.phpunit.cache
/.vscode
/.zed
/auth.json
/node_modules
/public/build
/public/hot
/public/storage
/storage/*.key
/storage/installed
/storage/pail
/vendor
Homestead.json
Homestead.yaml
Thumbs.db

.env.dusk.local

# Répertoires de travail temporaires des agents : un « gitlink » vers un worktree
# inexistant avait été committé (cf. #157), ce qui faisait échouer
# `git submodule foreach` à CHAQUE checkout du CI — un avertissement permanent,
# sans rapport avec le code.
.claude/worktrees/
